# Internal dev docs. Plans and handoffs record unfixed findings and internal
# process — they are never committed and never published. Matched in any
# directory, so a new one can't slip in from a subfolder either.
HANDOFF.md
PLAN.md

bin/
obj/
*.user
.vs/
.idea/
artifacts/
*.nupkg

# makepkg build artifacts
packaging/src/
packaging/pkg/
packaging/*.pkg.tar.zst
packaging/*.log
packaging/*.tar.gz
